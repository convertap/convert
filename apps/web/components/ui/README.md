# shadcn Primitives

shadcn/ui components are added here with:

```bash
npx shadcn@latest add <component>
```

Run the command from `apps/web` so `components.json` writes to the correct paths.

These files are source-owned, but they remain primitives. Keep product-specific behavior, API calls, feature flags, and domain vocabulary out of this directory.
