# Convert Product Components

Product components live here. They compose shadcn primitives from `../ui` and express Convert workflows such as lead cards, pipeline columns, contact timelines, follow-up prompts, campaign status, and WhatsApp window state.

Use this directory when a component contains product vocabulary or workflow behavior. If the component is a generic primitive, add or update it under `components/ui` instead.
