# Layout Components

Shared app shells, navigation, sidebars, headers, and responsive page frames live here.

Layouts may compose shadcn primitives and Convert product components, but they should not contain domain rules or direct API calls.
