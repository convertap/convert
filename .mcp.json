{
  "$comment": [
    "Project-scoped MCP servers. Committed on purpose: it contains no secrets.",
    "Every credential is referenced as ${VAR} and comes from Infisical at launch,",
    "so start Claude Code with:  infisical run --env=dev -- claude",
    "Without that wrapper the variable is unset and the server fails to authenticate,",
    "which is the intended failure mode: a missing secret should be loud, not silent."
  ],
  "mcpServers": {
    "notion": {
      "command": "npx",
      "args": ["-y", "@notionhq/notion-mcp-server"],
      "env": {
        "NOTION_TOKEN": "${NOTION_TOKEN}"
      }
    }
  }
}
